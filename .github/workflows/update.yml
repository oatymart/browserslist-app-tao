name: Update Browserslist database

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-browserslist-database:
    runs-on: ubuntu-latest
    steps:
      - name: Configure git
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Action"

      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Switch branch
        uses: peterjgrainger/action-create-branch@v2.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          branch: 'ci/update-browserslist'

      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: 14.x
      
      - name: Install packages
        run: npm ci
      
      - name: Update Browserslist database (old way)
        run: |
          npx browserslist@latest --update-db     
          git commit -am "ci: update browserslist database"
 
      - name: Create PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create --head "ci/update-browserslist" --title "Update browserslist database" --label "ci" --body "test"
